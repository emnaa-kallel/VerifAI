import requests

def reverse_image_search(image_url):
    """
    Exemple simple : recherche inversée via SerpAPI ou API similaire
    Retourne une liste de résultats avec URL et score de similarité
    """
    # Ici on simule la réponse pour le prototype
    results = [
        {"url": "https://source1.com", "similarity": 0.92},
        {"url": "https://source2.com", "similarity": 0.85}
    ]
    return results